# Pinned floors that are known-good. After the first successful deploy,
# run `pip freeze > requirements.txt` to lock exact versions for reproducibility.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
